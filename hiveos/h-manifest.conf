CUSTOM_NAME="fredminer"
CUSTOM_VERSION="0.2.1-dev"

# FredMiner reads the Flight Sheet variables directly in h-run.sh,
# so no generated config file is required.
CUSTOM_CONFIG_FILENAME=""

CUSTOM_LOG_BASENAME="/var/log/miner/fredminer/fredminer"
